import { FunctionComponent } from "react";
import { UserData } from "../types";

interface GameProps {
  userData: UserData;
}

const Game: FunctionComponent<GameProps> = ({ userData }) => {
  return <></>;
};

export default Game;
